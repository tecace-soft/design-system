# Alert
Centered modal dialog with title, body, and 1–2 actions (composes Button).

```jsx
<Alert title="삭제하시겠어요?" description="되돌릴 수 없습니다." primaryLabel="삭제" secondaryLabel="취소" onPrimary={del} />
```

`layout` horizontal|vertical stacks the action buttons.