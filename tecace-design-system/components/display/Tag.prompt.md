# Tag
Labelled token — optional colored dot and remove button. Use for applied filters / keywords.

```jsx
<Tag label="React" onRemove={()=>drop('React')} />
<Tag label="정규직" dotColor="var(--status-positive)" />
```