# ListRow
List cell: leading slot + title/description + trailing slot (icon, value, switch, chevron).

```jsx
<ListRow leading={<Avatar/>} title="김민아" description="프로덕트 디자이너" trailing={<Chevron/>} divider onClick={open} />
```