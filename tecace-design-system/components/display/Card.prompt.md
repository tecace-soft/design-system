# Card
Surface container. Outlined (1px inset border) or elevated (ambient shadow), radius 16.

```jsx
<Card variant="outlined" padding={20}>…</Card>
<Card variant="elevated" onClick={open}>…</Card>
```