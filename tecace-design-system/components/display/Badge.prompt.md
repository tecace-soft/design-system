# Badge
Notification dot or count. Standalone, or wrap a target to anchor it top-right.

```jsx
<Badge count={5}><BellIcon/></Badge>
<Badge dot><Avatar/></Badge>
```

Props: `count`, `dot`, `max` (default 99), `color`.