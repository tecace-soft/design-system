# Checkbox
Multi-select control with optional label and indeterminate state.

```jsx
<Checkbox checked={v} label="약관에 동의합니다" onChange={setV} />
<Checkbox indeterminate label="일부 선택" />
```

Sizes sm|md · `indeterminate`, `disable`.