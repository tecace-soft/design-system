# FloatingActionButton
Prominent floating primary action, usually bottom-right on mobile.

```jsx
<FloatingActionButton icon={<Plus/>} />
<FloatingActionButton icon={<Plus/>} label="글쓰기" />
```

Circular when icon-only; rounded-rect (extended) when `label` is set.