# Chip
Compact selectable/removable token for filters, tags, and quick choices.

```jsx
<Chip label="개발" active onClick={toggle} />
<Chip label="디자인" variant="alternative" />
```

Sizes xs|sm|md|lg · `variant` normal(outlined)|alternative(filled) · `active`, `disable`.