# IconButton
A square/circular button that shows only an icon. Provide `aria-label`.

```jsx
<IconButton icon={<Plus/>} variant="background" size="md" aria-label="추가" />
```

Variants: normal (bare), background (filled circle), outlined. Sizes sm|md|lg.