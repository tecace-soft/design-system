Multi-select chip — a pill with a leading check that toggles selected (blue) vs unselected (grey).

```jsx
<ChipMultiSelect label="React" state={on ? "selected" : "default"} onClick={toggle} />
```

- `size`: `"md"` | `"lg"`. `state`: `default` / `selected` / `disabled` / `selected disabled`.
