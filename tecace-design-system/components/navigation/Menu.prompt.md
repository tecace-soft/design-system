# Menu
Floating action list (popover / overflow menu). Position it yourself under the trigger.

```jsx
<Menu items={[{label:'수정'},{label:'공유'},{divider:true},{label:'삭제', danger:true}]} onSelect={run} />
```