# Pagination
Carousel dots (Page Indicator) or a numeric pager.

```jsx
<Pagination variant="dots" total={4} current={i} onChange={setI} />
<Pagination variant="numeric" total={8} current={p} onChange={setP} />
```